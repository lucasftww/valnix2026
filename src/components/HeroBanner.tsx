import { memo } from "react";

const HeroBannerComponent = () => {
  // Banners feature disabled - collection site_banners não existe
  return null;
};

export const HeroBanner = memo(HeroBannerComponent);
